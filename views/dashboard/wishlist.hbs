<h1>Wishlist</1h>

{{> toolbar }}
<ul>
  {{#each user.wishlist}}
    <li>Title : {{this.title}}</li>
    <li>genre : {{this.genre}}</li>
    <li>owner : {{this.owner.name}}</li>
    <br>
    <form action="/dashboard/{{_id}}/remove-wishlist" data-id="{{_id}}" method="post" class="remove-wishlist">
        <button type="submit"> Remove from wishlist</button></form>
  {{/each}}
   
</ul>


{{#unless user.wishlist.length}}
<p>You don't have any books in your wishlist !</p>
<a href="/all-books">Add a book to my wishlist</a>
{{else}}
<a href="/all-books">Add another book to my wishlist</a>
{{/unless}}