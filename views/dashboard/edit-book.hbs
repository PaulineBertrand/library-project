<h2>Got something wrong? Edit your book information</h2>
<form action="/dashboard/{{id}}/edit-book" method="post">
    <div>
        <label >Title</label>
        <input type="text" name="title" value={{title}}>
    </div>

    <div>
        <label >Author</label>
        <input type="text" name="author" value={{author}}>
    </div>

    <div>
        <label >Genre</label>
        {{!-- here I'd like the genre to be selected --}}
        <select type="text" name="genre">
            {{#each allGenres}}
                <option value={{this}}>{{this}}</option>
            {{/each}}
        </select>
    </div>

    <div>
        <label >Image</label>
        <input type="text" name="image">
    </div>

        <div>
        <label >Optional: what did you think about this book?</label>
        <input type="text" name="comment" value={{comment}}>
    </div>

    <button>OK</button>
</form>